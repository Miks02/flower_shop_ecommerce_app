@model FlowerShop.Web.Areas.Admin.Models.Orders.OrderDetailsViewModel

@{
    ViewBag.Title = $"Porudžbina #{Model.Order.OrderNumber}";
    Layout = "_AdminLayout";
}

<div id="user-content" class="p-4">
    @await Html.PartialAsync("_OrderDetails", Model)
</div>
